[project]
name = "interface"
version = "0.1.0"
description = "interface package"
readme = "README.md"
authors = [
    { name = "matthias springstein", email = "matthias.springstein@gmail.com" },
]
requires-python = ">=3.10"
dependencies = ["grpcio==1.71.0", "protobuf>=5.29.3"]

[tool.uv]
package = true